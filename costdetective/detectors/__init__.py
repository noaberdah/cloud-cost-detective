"""Waste detectors. Each module here scans one pattern and returns Findings.

Populated in Stage 2+ (ebs_unattached, ec2_idle, ...). Kept empty for now so
the synthetic pipeline can run end-to-end first.
"""
